# Database
DATABASE_URL="postgresql://boom_admin:password@localhost:5432/boom_v2"
POSTGRES_USER=boom_admin
POSTGRES_PASSWORD=changeme
POSTGRES_DB=boom_v2

# Redis
REDIS_URL="redis://:password@localhost:6379"
REDIS_PASSWORD=changeme

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme-generate-with-openssl-rand-base64-32

# Infomaniak OAuth (optional - configured via Settings UI in production)
# Note: In production, configure via Settings page instead of env vars
# Redirect URI: https://boom-new.boofactory.ch/api/auth/callback/infomaniak
# INFOMANIAK_CLIENT_ID=changeme
# INFOMANIAK_CLIENT_SECRET=changeme
# INFOMANIAK_ENABLED=false

# Auth Simple (Phase 1)
ADMIN_USERNAME=boo-team
ADMIN_PASSWORD_HASH=$2b$10$...changeme

# Notion API
NOTION_API_TOKEN=ntn_changeme
NOTION_DATABASE_ID=changeme

# Application
NODE_ENV=development
LOG_LEVEL=debug
ENABLE_ANALYTICS=false
NEXT_TELEMETRY_DISABLED=1

# Tailscale Photomatons (example)
PHOTOMATON_1_IP=100.x.x.x
PHOTOMATON_1_HOSTNAME=photomaton-1
PHOTOMATON_1_ROUTER_IP=100.x.x.y
PHOTOMATON_2_IP=100.x.x.x
PHOTOMATON_2_HOSTNAME=photomaton-2
PHOTOMATON_2_ROUTER_IP=100.x.x.y
